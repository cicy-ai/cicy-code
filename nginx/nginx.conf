events {}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  keepalive_timeout 65;

  server {
    listen 8009;
    server_name _;
    root /srv/cicy-landing;
    index index.html;

    location / {
      try_files $uri $uri/ /index.html;
    }
  }
}
