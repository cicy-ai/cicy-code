# Driven by CI (ide-extensions.yml patches pluginVersion from the ide-v* tag).
pluginVersion=0.0.0

# Target platform to build/verify against. 2024.1 = build 241.
platformType=IC
platformVersion=2024.1

# Plugin compatibility range (used in the built plugin.xml).
pluginSinceBuild=241
pluginUntilBuild=

org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
kotlin.stdlib.default.dependency=false
