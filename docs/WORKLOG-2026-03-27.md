# 2026-03-27 Worklog（归档）

这是当天的工作摘要归档，不是当前 SOP。

当前请以：

- `README.md`
- `skills/README.md`
- `docs/sop/v1/*`

为准。

## 当天留下来的几个有效结论

### 1. 终端 `clear` 问题曾被修过

当天定位到两层原因：

- tmux 默认终端类型
- PTY 子进程环境变量里的 `TERM`

这个结论仍有参考价值：遇到 WebTTY/ttyd 渲染异常，先查 tmux 和 shell 环境，不要只看前端。

### 2. 发布链路后来统一到版本同步脚本

那天明确了“版本号不能手改很多处”，后续代码已经收敛到：

- `npm/package.json` 作为版本源
- `python3 dev.py --bumpVersion <version>` 统一同步

### 3. Cloud Run / managed runtime 路径做过验证

当天验证过“托管运行时也能跑完整节点能力”这条方向，最后留下来的现实是：

- 仓库代码仍保留 managed runtime 与 Cloud Run 相关能力
- 本地开发主线仍然是 `python3 dev.py`
- 线上托管能力请按当前代码与当前环境变量配置判断，不要直接套用当天实验结论

### 4. `cicy-code` 与 `cicy-master` 的职责曾在这一天被拆开

这个边界今天仍然成立：

- `skills/cicy-code`：节点 API 客户端
- `skills/cicy-master`：本地注册表管理器

但文件路径与默认配置已经以当前仓库代码为准，不再沿用当天的旧写法。

## 这份归档现在怎么用

把它当成历史背景即可，主要用途是：

- 解释为什么仓库里同时存在 runtime、registry、tmux、skills 这些面
- 回看当时的一些设计取舍
- 帮助理解部分历史文件为何还留在仓库里

不要把这里的旧路径、旧镜像名、旧工作区目录名当成今天的操作说明。
