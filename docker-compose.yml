services:
  mysql:
    image: mysql:8.0
    container_name: cicy-mysql
    restart: always
    network_mode: host
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_TCP_PORT: ${MYSQL_PORT:-3306}
    volumes:
      - mysql-data:/var/lib/mysql
    command: >
      --default-authentication-plugin=mysql_native_password
      --innodb-flush-log-at-trx-commit=2
      --innodb-buffer-pool-size=128M
      --max-allowed-packet=64M
      --port=${MYSQL_PORT:-3306}
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 30s
      timeout: 5s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: cicy-redis
    restart: always
    network_mode: host
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --port ${REDIS_PORT:-6379}
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "${REDIS_PORT:-6379}", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3

  ide-dev:
    image: node:20-alpine
    container_name: cicy-ide-dev
    working_dir: /app
    network_mode: host
    volumes:
      - ./ide:/app
      - ide_modules:/app/node_modules
    command: sh -c "npm install && npm run dev"
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: cicy-nginx
    restart: always
    network_mode: host
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ide/dist:/usr/share/nginx/html:ro

  code-server:
    image: codercom/code-server:latest
    container_name: cicy-code-server
    restart: unless-stopped
    network_mode: host
    user: "1000:1000"
    environment:
      - TERM=xterm-256color
    volumes:
      - ${HOST_HOME:-/home/w3c_offical}:/home/w3c_offical
      - code-server-data:/home/coder/.local/share/code-server
    command: --bind-addr 0.0.0.0:${CODE_SERVER_PORT:-8002} --auth none

  mitmproxy:
    build: ./mitmproxy
    container_name: cicy-mitmproxy
    restart: unless-stopped
    network_mode: host
    environment:
      - REDIS_PORT=${REDIS_PORT:-6379}
      - API_PORT=${API_PORT:-8008}
    volumes:
      - ./mitmproxy/kiro_monitor.py:/app/kiro_monitor.py:ro
      - mitmproxy-data:/home/mitmproxy/.mitmproxy
    command: mitmdump -p ${MITMPROXY_PORT:-8003} --ssl-insecure --set block_global=false --set proxyauth=any -s /app/kiro_monitor.py

volumes:
  mysql-data:
  redis-data:
  ide_modules:
  code-server-data:
  mitmproxy-data:
